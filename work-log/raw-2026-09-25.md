# 工作日志 2026-09-25

---
### [01:05]
**来源：** Codex
**问题：** 将语音合成改为调用全局 edge-tts，而不使用 AIService 中的 edge_tts。
**解决：** 已改为直接调用全局 edge-tts，并将生成的音频转换为小智设备所需的 24 kHz PCM，聊天回复按文本段合成。语音设置已新增“本地 Edge TTS”并切换当前提供商；服务健康检查和实际语音合成均已通过。
**涉及文件：** src/web/api/voice_settings.rs、web-ui/src/data/tts-providers.ts、web-ui/src/pages/Settings/VoiceSettings.tsx
---

---
### [01:40]
**来源：** Codex
**问题：** 询问本地 SenseVoice Small V1 语音识别模型能否用于当前项目，并要求先回答、不修改代码。
**解决：** 已检查模型文件和项目现有 ASR 接口，确认该模型具备接入条件，但当前项目无法直接选用该目录。接入需要新增本地识别 Provider 并处理音频分段；本轮未修改代码或配置，也未运行模型验证识别效果。
**涉及文件：** 无
---

---
### [01:43]
**来源：** Codex
**问题：** 目前项目是否使用流式语音识别？
**解决：** 已确认项目默认模式会在收到第一帧语音时启动识别，并在录音过程中持续传入音频、接收结果。流式识别失败时会回退到整段识别；另一种模式则在录音结束后识别。
**涉及文件：** 无
---

---
### [01:58]
**来源：** Codex
**问题：** 将项目中的本地 SenseVoice Small 模型接入为可选 ASR Provider，同时保留现有 Provider。
**解决：** 已接入 SenseVoice 本地 ASR，并在 Web 设置中提供模型验证和首选 Provider 切换；当前首选仍为豆包。已重启服务并验证模型加载、实际语音识别、471 项 Rust 测试及前端构建和检查；本地模型文件已加入 Git 忽略规则。
**涉及文件：** .gitignore、src/sensevoice_asr.rs、src/xiaozhi_asr_llm_tts.rs、src/xiaozhi_asr_tts.rs、web-ui/src/data/asr-providers.ts
---

---
### [01:58]
**来源：** Codex
**问题：** 客户端以每帧 60 毫秒的 Opus 音频通过 WebSocket 发送时，haimen 的非流式本地 ASR 能否处理？
**解决：** 可以处理：haimen 会逐帧解码音频，本地 ASR 收集解码后的 PCM，并在录音结束后对整段音频识别。默认模式下，检测到有效语音后连续约 2.4 秒静音会触发判停和识别；录音期间不会返回中间识别结果。
**涉及文件：** 无
---

---
### [02:01]
**来源：** Codex
**问题：** 清理项目构建产物。
**解决：** 已清理构建目录和前端缓存，构建文件约 5.7 GiB。已确认运行中的服务正常，并保留仓库跟踪的前端发布文件、本地模型、已安装的二进制及代码改动。
**涉及文件：** 无
---

---
### [13:04]
**来源：** Codex
**问题：** 如何调用 macOS 系统自带的文字转语音服务？
**解决：** 无实质性变更。已说明可通过 macOS 的 say 命令朗读文本、选择声音和语速或导出音频；在 Rust CLI 中可启动 say 进程，原生集成可使用 AVSpeechSynthesizer。
**涉及文件：** 无
---

---
### [13:08]
**来源：** Codex
**问题：** 当前 TTS 是先合成文件再发送，还是直接发送音频流？
**解决：** 已确认默认对话模式会将合成的 PCM 音频块编码为 Opus 帧，并通过 WebSocket 逐帧发送。TTS 测试模式会先在内存中取得整段音频；edge_tts 提供商会先生成临时 MP3。发送后保存的 WAV 录音不作为设备音频的来源。
**涉及文件：** 无
---

---
### [13:10]
**来源：** Codex
**问题：** Mac 原生 TTS 是否支持边合成边发送音频流？
**解决：** 支持。macOS 原生语音合成 API 可在合成过程中回调音频缓冲区，收到后即可转换、编码并发送。接入当前音频链路时，需要转换为 24 kHz 单声道 PCM16，再编码为 Opus 帧；本轮未修改文件。
**涉及文件：** 无
---

---
### [13:10]
**来源：** Codex
**问题：** macOS 原生 TTS 支持哪些参数？
**解决：** 已说明 AVSpeechSynthesizer 支持音色、语速、音调、音量及语句前后停顿，并可用 IPA 标注控制个别词的发音。同时说明了可用音色的获取方式，以及采样率、声道数和 Opus 编码需要在音频输出后另行处理。
**涉及文件：** 无
---

---
### [13:12]
**来源：** Codex
**问题：** 能否读取当前默认使用的语音合成参数值？
**解决：** 已通过只读查询获取 Mac 系统默认音色及原生语音接口的语速、音调、音量和停顿默认值。已说明旧版接口的语速采用不同标尺，且这些值并非海门的 TTS 配置。
**涉及文件：** 无
---

---
### [13:13]
**来源：** Codex
**问题：** 系统自带的 TTS 会常驻内存吗？
**解决：** 无实质性变更
**涉及文件：** 无
---

---
### [13:30]
**来源：** Codex
**问题：** 在当前项目接入 macOS 原生 TTS、将五项参数以现有值为默认配置暴露到 Web 页面，并精简 TTS 和 ASR 选项。
**解决：** 已接入 macOS 原生 TTS，并将其音频接入现有的流式 Opus 发送链路；Web 页面已提供音色、语速、音调、音量及朗读停顿配置，并精简了 ASR/TTS 选项。相关测试、Rust 检查和前端构建均通过；尚未使用小智硬件验证播放。
**涉及文件：** build.rs、src/native_tts.rs、src/native_tts_helper.swift、web-ui/src/components/VoiceSelector.tsx
---

---
### [13:38]
**来源：** Codex
**问题：** 编译项目、重新启动服务，并在完成后清理编译产物。
**解决：** 已完成编译并重新启动服务，健康检查和语音合成测试均通过。随后清理了 Cargo 编译产物及部署临时文件，保留了运行中的可执行文件和网页构建产物。
**涉及文件：** 无
---

---
### [13:47]
**来源：** Codex
**问题：** 为什么客户端连接 haimen 服务时报错？
**解决：** 定位到本地 SenseVoice ASR 被错误要求提供 API Key，导致 `/xiaozhi/ws` 未挂载，客户端无法建立 WebSocket 连接。已修正判断并重启服务；回归测试通过，WebSocket 握手返回 101，健康检查正常，编译产物已清理。
**涉及文件：** src/gateway/mod.rs
---
